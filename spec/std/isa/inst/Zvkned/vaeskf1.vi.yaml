# Copyright (c) Qualcomm Technologies, Inc. and/or its subsidiaries.
# SPDX-License-Identifier: BSD-3-Clause-Clear

# yaml-language-server: $schema=../../../../schemas/inst_schema.json

$schema: "inst_schema.json#"
kind: instruction
name: vaeskf1.vi
long_name: Vector AES key schedule first round
description: |
  Performs the first round of AES key schedule computation on vector register vs2 using immediate value as round constant, storing results in vector register vd according to mask vm. This instruction is used in AES key expansion.
definedBy:
  extension:
    name: Zvkned
assembly: vd, vs2, imm
encoding:
  match: 1000101----------010-----1110111
  variables:
    - name: vs2
      location: 24-20
    - name: imm
      location: 19-15
    - name: vd
      location: 11-7
access:
  s: always
  u: always
  vs: always
  vu: always
data_independent_timing: true
operation(): |
  VectorState state = vector_state();
  if (state.sew != 32 || !vector_element_group_legal(state, 128, 4)) {
    raise(ExceptionCode::IllegalInstruction, mode(), $encoding);
  }

  Bits<4> round = imm[3:0];
  if (round == 0 || round > 10) {
    round[3] = ~round[3];
  }

  for (U32 group = CSR[vstart].VALUE / 4; group < CSR[vl].VALUE / 4; group++) {
    U32 element = group * 4;
    vector_write_eg128(vd, element,
      crypto_aes_key_schedule_128(vector_read_eg128(vs2, element), round));
  }

  CSR[vstart].VALUE = 0;
