# Copyright (c) Qualcomm Technologies, Inc. and/or its subsidiaries.
# SPDX-License-Identifier: BSD-3-Clause-Clear

# yaml-language-server: $schema=../../../../schemas/inst_schema.json

$schema: "inst_schema.json#"
kind: instruction
name: vmsne.vx
long_name: Vector mask not equal scalar
description: |
  Sets mask bits where elements in vs2 do not equal scalar in rs1, according to mask vm, and stores results in vd. Element width is determined by SEW setting. Supports vector masking and follows vector tail policy.
definedBy:
  extension:
    name: Zvl32b
assembly: vd, vs2, xs1, vm
encoding:
  match: 011001-----------100-----1010111
  variables:
    - name: vm
      location: 25-25
    - name: vs2
      location: 24-20
    - name: xs1
      location: 19-15
    - name: vd
      location: 11-7
access:
  s: always
  u: always
  vs: always
  vu: always
data_independent_timing: false
operation(): |
  check_v_ok_vtype($encoding);
  VectorState state = vector_state();
  # Read-modify-write: avoid setBit-on-copy ISS bug (see bits.hpp)
  Bits<VLEN> vd_bits = V[vd];
  for (U32 i = CSR[vstart].VALUE; i < CSR[vl].VALUE; i++) {
    if (v_mask_elem_active?(vm, i)) {
      vd_bits[i] = (v_elem_read(vs2, i, state.log2_sew)[state.sew-1:0] != X[xs1][state.sew-1:0]) ? 1'b1 : 1'b0;
    }
  }
  V[vd] = vd_bits;

  CSR[vstart].VALUE = 0;
  mark_v_state_dirty();
