# Copyright (c) Qualcomm Technologies, Inc. and/or its subsidiaries.
# SPDX-License-Identifier: BSD-3-Clause-Clear

# yaml-language-server: $schema=../../../../schemas/inst_schema.json

$schema: "inst_schema.json#"
kind: instruction
name: vsm3c.vi
long_name: Vector SM3 compression with immediate
description: |
  Performs SM3 compression operation on vector register vs2 using immediate value as round constant, storing results in vector register vd according to mask vm. This is used in the SM3 hash function compression phase.
definedBy:
  extension:
    name: Zvksh
assembly: vd, vs2, imm
encoding:
  match: 1010111----------010-----1110111
  variables:
    - name: vs2
      location: 24-20
    - name: imm
      location: 19-15
    - name: vd
      location: 11-7
access:
  s: always
  u: always
  vs: always
  vu: always
data_independent_timing: true
operation(): |
  VectorState state = vector_state();
  U32 group_registers = vector_register_group_registers(state);
  U32 destination = vd;
  U32 source = vs2;
  Boolean destination_overlaps_source =
    destination < source + group_registers && source < destination + group_registers;
  if (state.sew != 32 || !vector_element_group_legal(state, 256, 8) ||
      destination_overlaps_source) {
    raise(ExceptionCode::IllegalInstruction, mode(), $encoding);
  }

  Bits<5> round_group = imm;
  for (U32 group = CSR[vstart].VALUE / 8; group < CSR[vl].VALUE / 8; group++) {
    U32 element = group * 8;
    vector_write_eg256(vd, element, crypto_sm3_compress(
      vector_read_eg256(vd, element), vector_read_eg256(vs2, element), round_group));
  }

  CSR[vstart].VALUE = 0;
